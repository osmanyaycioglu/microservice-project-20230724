import React from 'react';

const OutWrite = ({text,min,max}) => {

    return (
        <>
            <h3>hello world : {text} min {min} max {max}</h3>
        </>
    );
};

export default OutWrite;